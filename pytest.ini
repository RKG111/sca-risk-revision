[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts = -ra --strict-markers
markers =
    live: requires a running stack (Ollama / Joern / MCP SSE); deselected by default
filterwarnings =
    ignore::DeprecationWarning
