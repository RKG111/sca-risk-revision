# Deprecated — Risk Assessment Agent (v1)

Previous agent architecture (`api/`, `core/`, and related tests). Superseded by the v2
layout at the repository root (`app/`, `agent/`, `skills/`).

Do not import from this package in new code. Kept for reference while skills and
scoring are re-implemented.
